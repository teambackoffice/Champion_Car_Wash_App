import 'package:flutter/material.dart';

class PreBookingButton extends StatelessWidget {
  const PreBookingButton({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        
      ),
      body: Center(
        child: Text("Pre PreBookingButton"),
      ),
    );
  }
}