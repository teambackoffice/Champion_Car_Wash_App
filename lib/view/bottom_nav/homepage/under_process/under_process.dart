import 'package:flutter/material.dart';

class UnderProcessScreen extends StatelessWidget {
  const UnderProcessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Under Process Screen"),
      ),
    );
    
  }
}