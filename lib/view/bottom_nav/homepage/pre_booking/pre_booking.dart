import 'package:flutter/material.dart';

class PreBookingScreenContainer extends StatelessWidget {
  const PreBookingScreenContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Pre PreBookingScreenContainer Button"),
      ),
    );
  }

    
}